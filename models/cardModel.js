class CardModel {
    constructor(type, value, symbol, color){
         this.type = type;
         this.value = value;
         this.symbol = symbol;
         this.color = color;
    }
}

module.exports = CardModel;