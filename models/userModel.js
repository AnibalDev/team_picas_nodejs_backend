const CardModel = require('./cardModel');

class Player {
    constructor(id, nombre, turno, mano){
         this.type = type;
         this.value = value;
         this.symbol = symbol;
         this.color = color;
         this.mano = [];
    }
    asignarMano(mano) {
        this.mano = mano;
      }
    
}

